<div>
  <h1>Select A Student</h1>
  <% @students.each do |student| %>
    <div>
      <ul>
        <li>
        <%= link_to student.to_s, student_path(student) %>
        </li>
      </ul>
    </div>
  <% end %>
</div>
